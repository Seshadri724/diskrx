[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "diskrx"
version = "0.1.0"
description = "Intelligent disk diagnostics and storage observability for SREs"
authors = [
    {name = "Seshadri Naidu Vangapandu", email = "seshuvangapandu@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.10"
keywords = ["disk", "diagnostics", "sre", "observability", "storage", "cli"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Monitoring",
    "Topic :: System :: Systems Administration",
]
dependencies = [
    "click>=8.0",
    "rich>=13.0",
    "textual>=0.50",
    "psutil>=5.9.0",
    "numpy>=1.24.0",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0.0",
    "pytest-mock>=3.10.0",
]

[project.urls]
Homepage = "https://github.com/Seshadri724/dxcli"
Repository = "https://github.com/Seshadri724/dxcli"
Issues = "https://github.com/Seshadri724/dxcli/issues"

[project.scripts]
dxcli = "dxcli.cli:cli"

[tool.setuptools.package-data]
dxcli = ["outputs/*.tcss"]
